# Source handoff status

Repository-side cleanup, checksum enforcement, extraction, verification, and Windows build wrappers are complete on branch `clean-development-source-2.18.1`.

The remaining transfer step is the binary archive itself, because the active GitHub connector supports repository text mutations but does not expose arbitrary binary upload through the contents action.

Use the exact locally delivered archive and checksum recorded throughout this branch. No alternative or older archive is accepted.
