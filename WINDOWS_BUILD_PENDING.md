# Windows build pending

The repository is prepared to consume the verified clean source archive and build version 2.18.1 on Windows x64. The build remains pending because GitHub does not start hosted runners while the account is locked for billing.

Once a Windows runner is available, run the existing workflow or execute `tools/BUILD_WINDOWS.ps1` on Windows 10/11 x64 with Python 3.11 and Inno Setup 6.
